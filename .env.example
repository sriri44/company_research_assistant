# This project has no root-level runtime env vars — each app owns its own:
#
#   backend/.env.example   — FastAPI service configuration
#   frontend/.env.example  — Vite/React client configuration
#
# See config/environments/ for how they relate to each other per deploy
# target, and docs/ARCHITECTURE.md §13 for the configuration strategy.
