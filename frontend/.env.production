# Production build config — loaded automatically by `npm run build`
# (Vite uses .env.production for the "production" mode). Safe to commit:
# this is a public API endpoint, not a secret.

VITE_API_BASE_URL=https://company-research-assistant-backend-cgk8.onrender.com
