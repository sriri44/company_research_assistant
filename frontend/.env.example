# Copy to .env and fill in your backend URL. Never commit .env.
# Vite only exposes variables prefixed VITE_ to client code.
# Local dev example: VITE_API_BASE_URL=http://localhost:8000
# Production value lives in frontend/.env.production.

VITE_API_BASE_URL=
