"""Application-wide constants that don't belong to any single feature.

Feature-specific constants (e.g., opportunity scoring thresholds) live next
to that feature instead of here.
"""

from __future__ import annotations

API_VERSION = "v1"
DEFAULT_PAGE_SIZE = 20
MAX_PAGE_SIZE = 100
