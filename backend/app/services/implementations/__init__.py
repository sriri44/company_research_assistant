"""Concrete service implementations.

Empty in Phase 1 by design. Each file added here (e.g.
`default_search_service.py`) implements exactly one interface from
`app.services.interfaces` and is wired to it in `app.core.container`. No
route or other service should import from this package directly — always
depend on the interface.
"""
